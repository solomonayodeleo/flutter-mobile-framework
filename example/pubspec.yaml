name: flutter_mobile_ui_library_example
description: Example app for the Flutter Mobile UI Library
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mobile_ui_library:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

